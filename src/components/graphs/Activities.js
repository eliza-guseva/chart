import React from 'react';
import RunPaceTime from './activities/RunPaceTime';

const Activities = ({}) => {
    return (
        <div>
            <RunPaceTime />
        </div>
    );
}
export default Activities;
